/** Obstacle `type` used to register a floor's points in the Python worker, since floors are registered as a specific kind of obstacle to reuse its distance-to-cable calculation. */
export const FLOOR_OBSTACLE_TYPE = 'floor';
