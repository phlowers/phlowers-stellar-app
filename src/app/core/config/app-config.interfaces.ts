/** Runtime configuration written to `assets/config/app-config.json` at deploy time. */
export interface AppConfig {
  /** Default UI language to activate at startup (e.g. `'fr'`, `'en'`). */
  defaultLang: string;
}
